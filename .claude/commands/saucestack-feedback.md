---
description: Send a generalizable learning from this project back to the saucestack starter as a PR (or an issue). The upstream counterpart to /compound.
argument-hint: [the learning, or leave blank to review recent learnings]
---

Run the `saucestack-feedback` skill.

If $ARGUMENTS describes a specific learning, upstream that one. Otherwise, review this session's `compound-learning` outputs and `.claude/learnings.jsonl` for generalizable candidates and propose them.

Read `.claude/starter.json` for the target starter repo. Open a PR for a concrete change (preferred) or an issue for a proposal. Classify strictly — generalizable only; project-specific lessons stay local via `/compound`. Never merge; a human does.
